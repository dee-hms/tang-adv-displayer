import React from 'react';
import { RowWrapperProps } from '@patternfly/react-table';
import { Point } from './helpers';
export interface TreeTableRow extends RowWrapperProps {
    [index: string]: any;
    level: number;
    isTreeOpen?: boolean;
    point?: Point;
    posinset?: string | number;
    treeParent: number;
}
export interface TreeRowWrapperProps {
    className?: string;
    row?: TreeTableRow;
    rows?: TreeTableRow[];
}
declare const TreeRowWrapper: React.FunctionComponent<TreeRowWrapperProps>;
export default TreeRowWrapper;
