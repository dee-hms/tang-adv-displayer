import { connect } from 'react-redux';
import Portal from '../Portal';
import { clearNotifications, removeNotification } from '../redux/actions/notifications';
var mapStateToProps = function (_a, initialProps) {
    var notifications = _a.notifications;
    return ({
        notifications: initialProps.notifications || notifications,
    });
};
var mapDispatchToProps = function (dispatch, initialProps) { return ({
    removeNotification: initialProps.removeNotification ? initialProps.removeNotification : function (id) { return dispatch(removeNotification(id)); },
    onClearAll: initialProps.clearNotifications ? initialProps.clearNotifications : function () { return dispatch(clearNotifications()); },
}); };
export var NotificationPortal = connect(mapStateToProps, mapDispatchToProps)(Portal);
export { default as Portal } from '../Portal';
export default NotificationPortal;
//# sourceMappingURL=NotificationPortal.js.map