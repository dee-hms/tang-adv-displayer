export function findElementByOuiaId() {
    Cypress.Commands.add('ouiaId', { prevSubject: 'optional' }, function (subject, item, el) {
        if (el === void 0) { el = ''; }
        var attr = "".concat(el, "[data-ouia-component-id=\"").concat(item, "\"]");
        return subject ? cy.wrap(subject).find(attr) : cy.get(attr);
    });
}
export function findElementByOuiaType() {
    Cypress.Commands.add('ouiaType', { prevSubject: 'optional' }, function (subject, item, el) {
        if (el === void 0) { el = ''; }
        var attr = "".concat(el, "[data-ouia-component-type=\"").concat(item, "\"]");
        return subject ? cy.wrap(subject).find(attr) : cy.get(attr);
    });
}
//# sourceMappingURL=CustomCommands.js.map