import { jsx as _jsx } from "react/jsx-runtime";
import { mount } from '@cypress/react';
import { SortBy } from '..';
describe('SortBy component', function () {
    it('renders empty component', function () {
        mount(_jsx(SortBy, {}));
        cy.get('.pf-c-button');
    });
    it('renders with direction set', function () {
        mount(_jsx(SortBy, { direction: "desc" }));
        cy.get('.pf-c-button');
    });
    it('onSortChange is called', function () {
        var sortSpy = cy.spy().as('sortSpy');
        mount(_jsx(SortBy, { direction: "desc", onSortChange: sortSpy }));
        cy.get('.pf-c-button').click();
        cy.get('@sortSpy').should('have.been.called');
    });
});
//# sourceMappingURL=SortBy.spec.ct.js.map