import { IModule } from '@scalprum/core';
export declare type ModuleDefinition = {
    appName?: string;
    scope: string;
    module: string;
    processor?: (item: any) => string;
};
export declare function useLoadModule({ appName, scope, module, processor }: ModuleDefinition, defaultState: any, options?: {
    skipCache?: boolean;
}): [IModule | undefined, Error | undefined];
